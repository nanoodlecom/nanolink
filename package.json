{
  "name": "nanolink",
  "private": true,
  "version": "0.2.0",
  "description": "First-party, zero-analytics link shortener for nanoodle share links — Cloudflare Worker + KV, body-bounce instead of Location redirects, rich OG link previews.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test"
  }
}
